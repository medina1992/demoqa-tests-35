plugins {
    id 'java-library'
}

repositories {
    mavenCentral()
}

dependencies {
    // JUnit 5
    testImplementation "org.junit.jupiter:junit-jupiter:5.10.0"
    testImplementation "org.junit.jupiter:junit-jupiter-params:5.10.0"
    testImplementation 'com.codeborne:selenide:7.8.1' // Убедитесь, что используете актуальную версию Selenide
    testImplementation 'org.seleniumhq.selenium:selenium-java:4.30.0' // Проверьте версию Selenium// Добавлена зависимость для параметризированных тестов

    // Selenide
    testImplementation "com.codeborne:selenide:7.8.1"

    // SLF4J (для логирования)
    testImplementation 'org.slf4j:slf4j-simple:2.0.9'
    testImplementation 'org.junit.jupiter:junit-jupiter-api:5.10.0'
    testImplementation 'org.junit.jupiter:junit-jupiter-engine:5.10.0'
    testImplementation 'com.codeborne:selenide:7.8.1'
    testImplementation 'org.slf4j:slf4j-simple:2.0.9'
    testImplementation "com.codeborne:selenide:7.8.1"
    testImplementation "org.seleniumhq.selenium:selenium-java:4.30.0"

}

test {
    useJUnitPlatform()
}

